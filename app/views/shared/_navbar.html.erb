<nav class="navbar navbar-expand-lg bg-body-tertiary bg-light">
  <div class="container-fluid">
    <%= link_to "Discova", root_path, class: "navbar-brand fs-2 fw-bold font-monospace" %>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Home", root_path, class: "nav-link active text-secondary" %>
        </li>
        <li class="nav-item">
          <%= link_to "About", about_path, class: "nav-link active text-secondary" %>
        </li>

        <li class="nav-item">
          <%= link_to "Write", new_post_path, class: "nav-link active text-secondary" %>
        </li>
        <%= render "/shared/notifications" %>
      </ul>
      
      <ul class="mx-5 mt-2">
        <% if user_signed_in? %>
          Hello
          <strong><%= current_user.name %></strong>
        <% end %>
      </ul>
      <ul class="navbar-nav">
        <%= render "/shared/session_manager" %>
      </ul>

    </div>
  </div>
</nav>
